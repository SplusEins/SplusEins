platform: linux
image_resource:
  type: docker-image
  source: {repository: alpine, tag: 11}

inputs:
  - name: repository
outputs:
  - name: npm-install-result
run:
  path: repository/concourse/install.sh
