platform: linux
image_resource:
  type: docker-image
  source: {repository: node, tag: alpine}

inputs:
  - name: repository
outputs:
  - name: npm-install-result
run:
  path: repository/concourse/install.sh
