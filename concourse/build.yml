platform: linux
image_resource:
  type: docker-image
  source: {repository: node}

inputs:
  - name: sources
  - name: node_modules
outputs:
  - name: build-result
run:
  path: sh
  args:
    - -exc
    - |
      mv cache/node_modules src
      cd src && npm rebuild node-sass && npm run build
