resources:
  - name: sources
    type: git
    source:
      uri: https://github.com/spluseins/spluseins.git
      branch: cli-setup

jobs:
- name: build
  plan:
  - get: sources
    trigger: true
  - task: install
    file: sources/concourse/install.yml
